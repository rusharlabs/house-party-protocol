{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "health-kit",
  "displayName": "Health Kit",
  "version": "1.3.1",
  "description": "Config-driven service health probe (HTTP or local command) + a statusline segment with per-service detail. Cache-first: the statusline never touches the network, it only reads the cache that health_probe.py writes.",
  "author": { "name": "Max Parisi", "url": "https://rusharlabs.com" },
  "license": "MIT",
  "keywords": ["health", "statusline", "monitoring", "probe", "portable", "config-driven"],
  "hooks": "./hooks/hooks.json"
}
