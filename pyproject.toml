[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "house-party-protocol"
version = "2.4.0"
description = "A local-first reliability harness for coding agents."
requires-python = ">=3.10"  # Why: a matriz de CI cobre 3.10-3.13. O codigo nao usa sintaxe
# exclusiva de 3.10 e provavelmente roda em 3.9, mas nada mede isso — entao nao se promete.
dependencies = []

[project.scripts]
hpp = "hpp.cli:main"

[tool.setuptools]
packages = ["hpp"]
